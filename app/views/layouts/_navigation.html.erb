<nav class="navbar navbar-expand-lg navbar-dark bg-primary" style="background-color: #e3f2fd;">
<a class="navbar-brand" href="">
<%= image_tag 'fb.png', :size => "30x30"  %>

</a>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if user_signed_in? %>
      <li class="nav-item active">
        <%= link_to "Home", user_posts_path(current_user), class:"nav-link" %>
      </li>
      <% end %>

      <% if !admin_signed_in? && !user_signed_in? %>
      <li class="nav-item active">
        <%= link_to "Admin sign in", new_admin_session_path, class:"nav-link" %>
      </li>
      <li class="nav-item active">
        <%= link_to "User sign in", new_user_session_path, class:"nav-link" %>
      </li>
      <% end %>

      <% if user_signed_in? %>
      <li class="nav-item active">
        <%= link_to "Your Profile", user_path(current_user), class:"nav-link" %>
      </li>
      <% end %>


      <% if user_signed_in? %>
        <li class="nav-item active">
          <%= link_to "Sign Out", destroy_user_session_path, data: { turbo_method: :delete }, class:"nav-link" %>
        </li>
      <% end %>

      <% if admin_signed_in? %>
      <li class="nav-item active">
        <%= link_to "Sign Out", destroy_admin_session_path, data: { turbo_method: :delete }, class:"nav-link" %>
      </li>
    <% end %>

      
    </ul>

    <% if user_signed_in? %>
      <form class="form-inline my-2 my-lg-0">
        <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-info my-2 my-sm-0" type="submit">Search</button>
      </form>
    <% end %>
  </div>
</nav>
